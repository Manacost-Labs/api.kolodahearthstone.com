[Unit]
StartLimitIntervalSec=8h
StartLimitBurst=4

[Service]
# wiki.gg may answer with HTTP 429 during a large rolling refresh. The parser
# exits with EX_TEMPFAIL (75), releases the global lock, and retries later.
Restart=on-failure
RestartSec=45m
